# Experiment Validation Gallery

This directory contains visual evidence of the system's performance, environment configuration, and repository integrity. These artifacts validate the results reported in the `results/` directory.

---

## Validation Artifacts

### 1. **Core Result Validation** (`screenshot.PNG`)
* **Description**: Captures the final terminal output of the `src/eval.py` execution.
* **Key Outcome**: Confirms the **88% Accuracy** and **0.85 F1-Score** on the GTSRB test set.

### 2. **Environment Verification** (`pip list.PNG`)
* **Description**: A full list of the Python dependencies installed in the local environment.
* **Key Outcome**: Confirms successful installation of `torch`, `torchvision`, `scikit-learn`, and `opencv-python`.

### 3. **Repository Integrity** (`dir-s-b.PNG`)
* **Description**: A recursive directory listing of the project.
* **Key Outcome**: Verifies that the local deployment matches the structured pipeline required for reproducibility.

---
*Verified by Team TAURATAPHI | March 28, 2026*
